destructing

eloquent, p.79f./defintive, ch.3.10.3/8.3.5

- extracts one or more vals from an arr (1) or obj (2) and stores them into one or more vars
- if var-names differ, varName: newVarName

EXAMPLE
(1)
function vectorAdd([x1, y1], [x2, y2]) {
    return [x1 + x2, y1 + y2];
}
vectorAdd([1, 2], [3, 4]) // => [4, 6]

(2)
let {name: n, age}= {name: "joe", age: 39}
console.log(n, age); // joe 39

let b = [1, 2, 3]
let [x, y, z] = b;
