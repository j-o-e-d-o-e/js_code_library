object->creation

definitive, ch.6

three ways to create obj
    (1) obj literal
        - shorthand props: omit key if key-name equals var-name (ES6)
        - shorthand methods: omit function-keyword
    (2) 'new' followed by constructor
        - since ES/JS 2015
    (3) Object.create()
        - returns newly created obj that inherits from specified prototype obj

EXAMPLE
(1)
let a = {
    b: 1, c: 3, multi() {
        return this.b * this.c;
    }
}
let {b, c} = a;
console.log(b, c) // -> 1 3
console.log(a.multi()) // -> 3
(2)
class Rabbit {
    constructor(type = "black") {
        this.type = type;
    }
    speak(line) {
        console.log(`The ${this.type} rabbit says '${line}'`);
    }
}
let rabbit = new Rabbit("white");
rabbit.speak("omg"); // The white rabbit says 'omg'
new Rabbit("purple").speak("yeah"); // The purple rabbit says 'yeah'
(3)
let o = Object.create({x: 1, y: 2});
console.log(o.x); // 1
