object->get/set

definitive, ch.6.10.6

- private properties are declared with underscore
- getter/setter used e.g. for sanity checks
    - funcs are executed when a prop with the same name is accessed/assigned
- causes implicit invocation, when querying or setting the value of its properties

EXAMPLE
const box = {
    locked: true,
    _content: ["gold piece"],
    get content() {
        if (this.locked) throw new Error("Locked!");
        return this._content;
    },
    set content(val) {
        this._content.push(val);
    }
};

box.content = "diamond"; // implicit invoke of setter
console.log(box._content); // [ 'gold piece', 'diamond' ]
try {
    console.log(box.content) // implicit invoke of getter
} catch (e) {
    console.log(e.message) // Locked!
}
